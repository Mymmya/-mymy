app = "moba-vps-pass"
primary_region = "ams"

[build]

[[services]]
  internal_port = 22 # Обязательно совпадает с веб-панелью
  protocol = "tcp"
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [[services.ports]]
    port = 22
